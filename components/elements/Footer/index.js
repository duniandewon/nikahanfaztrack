import styles from "./Footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>This website is designed and built by:</p>
      <p>The bride and the groom</p>
    </footer>
  );
};

export default Footer;
